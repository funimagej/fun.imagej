(ns plugins.Scripts.Plugins.fun.imagej.Experimental.write-ops
  (:require [fun.imagej.ops :as ops]))

(defn create-op
  "Create an op from a function."
  [ctx f]
  ; Could create PluginInfo
  ; Send that to PluginService
  )
